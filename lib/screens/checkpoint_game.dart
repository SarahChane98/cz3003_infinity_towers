// States: retrieve from backend: which floor is the user currently on;

// Send request to backend (tower name, checkpoint number, current floor...) to get corresponding question

// Pop up question for user to try

// User selects options / fill in blanks (depending on question type)

// Check whether answer is correct locally or remotely (?)

// Send feedback (whether user answered the qn correctly or wrongly) to backend and get instructions from backend (eliminated from game / demoted one level / promoted one level / finished checkpoint)

